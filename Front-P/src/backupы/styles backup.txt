/* Reset / базовые */
html, body, #root {
  height: 100%;
  margin: 0;
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  box-sizing: border-box;
}

/* Global */
button {
  cursor: pointer;
}

/* Layout / Positioning */
.app-root {
  padding: 24px;
  font-family: sans-serif;
  display: grid;
  grid-template-columns: 200px 1fr;
  gap: 16px;
}

.app-header {
  grid-column: 1 / -1;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* TitleEditor */
.title-editor {
  margin-bottom: 16px;
}

.title-editor input {
  width: 100%;
  font-size: 20px;
  padding: 8px;
  box-sizing: border-box;
}

/* SlideList (block model) */
.slide-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 8px;
  max-height: 240px;
  overflow-y: auto;
  align-content: flex-start;
  background: #fafafa;
  border: 1px solid #e0e0e0;
  border-radius: 6px;
  box-sizing: border-box;
}

.slide-thumb {
  width: 100px;
  height: 70px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  box-sizing: border-box;
  border: 1px solid #ccc;
  background-size: cover;
  background-position: center;
  transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
}

.slide-thumb:hover {
  transform: translateY(-4px);
  box-shadow: 0 6px 12px rgba(0,0,0,0.08);
}

.slide-thumb.active {
  border: 3px solid #007acc;
}

/* Toolbar */
.toolbar {
  display: flex;
  gap: 8px;
  align-items: center;
  flex-wrap: wrap;
}

.toolbar label {
  display: flex;
  align-items: center;
  gap: 6px;
}

.toolbar .bg-input {
  width: 40px;
  height: 28px;
  padding: 0;
  border: none;
  background: transparent;
}

/* Workspace (positioning, block model, content) */
.workspace {
  position: relative;
  width: 600px;
  height: 400px;
  border: 1px solid #999;
  margin-bottom: 16px;
  box-sizing: border-box;
  overflow: hidden;
  background-repeat: no-repeat;
  background-size: cover;
}

/* Draggable element base */
.workspace .el {
  position: absolute;
  cursor: pointer;
  box-sizing: border-box;
  overflow: visible;
  padding: 4px;
  transition: transform 0.12s ease, box-shadow 0.12s ease;
  touch-action: none;
  user-select: none;
  will-change: left, top, transform;
}

/* Hover effect for non-dragging elements */
.workspace .el:not(.is-dragging):hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 10px rgba(0,0,0,0.06);
}

/* Disable transitions/hover while dragging */
.workspace .el.is-dragging {
  transition: none !important;
  transform: none !important;
  box-shadow: none !important;
}

/* Text element specifics */
.workspace .el.text {
  white-space: pre-wrap;
  overflow: hidden;
}

/* Image inside element */
.workspace .el img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  pointer-events: none;
}

/* Handles (resize dots) - improved hit area and z-index */
.workspace .el .handle {
  display: none;
  width: 16px;
  height: 16px;
  background: #fff;
  border: 1px solid #999;
  border-radius: 3px;
  box-shadow: 0 1px 2px rgba(0,0,0,0.08);
  position: absolute;
  z-index: 2000;
  pointer-events: auto;
  box-sizing: border-box;
  touch-action: none;
}

/* Show handles when element is selected, editing, or focused */
.workspace .el.selected .handle,
.workspace .el.editing .handle,
.workspace .el:focus-within .handle {
  display: block;
}

/* Handle positions and cursors (offset so hit area centered outside border) */
.handle-nw { left: -8px; top: -8px; cursor: nwse-resize; }
.handle-ne { right: -8px; top: -8px; cursor: nesw-resize; }
.handle-se { right: -8px; bottom: -8px; cursor: nwse-resize; }
.handle-sw { left: -8px; bottom: -8px; cursor: nesw-resize; }

.handle-n { left: 50%; transform: translateX(-50%); top: -8px; cursor: ns-resize; }
.handle-s { left: 50%; transform: translateX(-50%); bottom: -8px; cursor: ns-resize; }
.handle-e { right: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }
.handle-w { left: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }

/* Handle hover/focus contrast */
.workspace .el .handle:hover,
.workspace .el .handle:focus {
  background: #f7f9fc;
  border-color: #666;
  box-shadow: 0 2px 6px rgba(0,0,0,0.15);
}

/* Inline edit input */
.workspace input.inline-edit {
  display: block;
  width: 100%;
  min-width: 0;
  box-sizing: border-box;
  padding: 6px 8px;
  border: 1px solid #bbb;
  border-radius: 4px;
  background: #fff;
  outline: none;
}

/* Accessibility: larger hit area when using touch (media query) */
@media (pointer: coarse) {
  .workspace .el .handle {
    width: 24px;
    height: 24px;
    left: -12px;
    top: -12px;
  }
  .handle-ne { right: -12px; top: -12px; }
  .handle-se { right: -12px; bottom: -12px; }
  .handle-sw { left: -12px; bottom: -12px; }
  .handle-n { top: -12px; }
  .handle-s { bottom: -12px; }
  .handle-e { right: -12px; }
  .handle-w { left: -12px; }
}

/* Small responsive tweak */
@media (max-width: 800px) {
  .app-root {
    grid-template-columns: 1fr;
  }
  .workspace {
    width: 100%;
    height: 320px;
  }
  .slide-thumb {
    width: 86px;
    height: 60px;
  }
}

.slide-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 8px;
}

.slide-row-wrap {
  display: flex;
  flex-direction: column;
}

.slide-thumb {
  height: 64px;
  width: 140px;
  border-radius: 4px;
  box-shadow: 0 0 0 1px rgba(0,0,0,0.08) inset;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: grab;
  user-select: none;
  transition: transform 120ms ease, opacity 120ms ease;
}

.slide-thumb.active {
  box-shadow: 0 0 0 2px #3b82f6;
}

.slide-thumb.dragging {
  opacity: 0.5;
  transform: scale(1.02);
  cursor: grabbing;
}

.slide-placeholder {
  height: 8px;
  width: 100%;
  background: rgba(60, 128, 255, 0.12);
  border: 1px dashed rgba(60, 128, 255, 0.25);
  margin: 4px 0;
  border-radius: 3px;
}

/* Layout for element container using CSS variables for dynamic values */
.el {
  position: absolute;
  left: var(--el-left, 0px);
  top: var(--el-top, 0px);
  width: var(--el-width, 100px);
  height: var(--el-height, 100px);
  box-sizing: border-box;
  padding: 4px;
  touch-action: none;
  user-select: none;
  cursor: pointer;
  overflow: visible;
  transition: transform 0.12s ease, box-shadow 0.12s ease;
  will-change: left, top, transform;
}

/* states */
.el.is-dragging {
  transition: none !important;
  transform: none !important;
  box-shadow: none !important;
}
.el.selected { /* visible selection state */ }
.el.editing { /* editing state */ }

/* image + text content inside element */
.el .content {
  width: 100%;
  height: 100%;
  pointer-events: none;
  overflow: hidden;
  display: block;
}
.el img.content {
  width: 100%;
  height: 100%;
  object-fit: fill;
  object-position: center;
  display: block;
}

/* inline edit input */
.inline-edit {
  display: block;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
  padding: 6px 8px;
  border: 1px solid #bbb;
  border-radius: 4px;
  background: #fff;
  outline: none;
}

/* Handles (resize dots) */
.handle {
  display: none;
  width: 16px;
  height: 16px;
  background: #fff;
  border: 1px solid #999;
  border-radius: 3px;
  box-shadow: 0 1px 2px rgba(0,0,0,0.08);
  position: absolute;
  z-index: 2000;
  pointer-events: auto;
  box-sizing: border-box;
  touch-action: none;
}

/* show handles when element selected / editing / focus-within */
.el.selected .handle,
.el.editing .handle,
.el:focus-within .handle {
  display: block;
}

/* Handle positions and cursors */
.handle-nw { left: -8px; top: -8px; cursor: nwse-resize; }
.handle-ne { right: -8px; top: -8px; cursor: nesw-resize; }
.handle-se { right: -8px; bottom: -8px; cursor: nwse-resize; }
.handle-sw { left: -8px; bottom: -8px; cursor: nesw-resize; }

.handle-n { left: 50%; transform: translateX(-50%); top: -8px; cursor: ns-resize; }
.handle-s { left: 50%; transform: translateX(-50%); bottom: -8px; cursor: ns-resize; }
.handle-e { right: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }
.handle-w { left: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }

/* accessibility for coarse pointers */
@media (pointer: coarse) {
  .handle { width: 24px; height: 24px; left: -12px; top: -12px; }
  .handle-ne { right: -12px; top: -12px; }
  .handle-se { right: -12px; bottom: -12px; }
  .handle-sw { left: -12px; bottom: -12px; }
  .handle-n { top: -12px; }
  .handle-s { bottom: -12px; }
  .handle-e { right: -12px; }
  .handle-w { left: -12px; }
}

/* Workspace base */
.workspace {
  /* фон задаётся переменной из компонента: --workspace-bg */
  background: var(--workspace-bg, #ffffff);
  position: relative;
  width: 600px;
  height: 400px;
  border: 1px solid #999;
  margin-bottom: 16px;
  box-sizing: border-box;
  overflow: hidden;
  background-repeat: no-repeat;
  background-size: cover;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Элемент контейнера: позиция/размер через CSS-переменные */
.el {
  position: absolute;
  left: var(--el-left, 0px);
  top: var(--el-top, 0px);
  width: var(--el-width, 100px);
  height: var(--el-height, 100px);
  box-sizing: border-box;
  padding: 4px;
  touch-action: none;
  user-select: none;
  cursor: pointer;
  overflow: visible;
  transition: transform 0.12s ease, box-shadow 0.12s ease;
  will-change: left, top, transform;
  background: transparent;
}

/* Состояния */
.el.is-dragging {
  transition: none !important;
  transform: none !important;
  box-shadow: none !important;
}
.el.selected { outline: none; /* визуализация через handles */ }
.el.editing { outline: none; }

/* Контент внутри элемента */
.el .content {
  width: 100%;
  height: 100%;
  pointer-events: none;
  overflow: hidden;
  white-space: pre-wrap;
  word-break: break-word;
  display: block;
  box-sizing: border-box;
  font-size: var(--el-font-size, 16px);
  font-family: var(--el-font-family, inherit);
}

/* Текстовый элемент (доп. класс) */
.el.text { /* стиль наследуется от .el .content */ }

/* Картинка внутри элемента */
.el .el-image,
.el img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
  background-color: #f3f7ff;
  display: block;
  pointer-events: none;
}

/* Inline edit input */
.inline-edit {
  display: block;
  width: 100%;
  height: 100%;
  min-width: 0;
  box-sizing: border-box;
  padding: 6px 8px;
  border: 1px solid #bbb;
  border-radius: 4px;
  background: #fff;
  outline: none;
  font-size: inherit;
  font-family: inherit;
}

/* Resize handles */
.handle {
  display: none;
  width: 16px;
  height: 16px;
  background: #fff;
  border: 1px solid #999;
  border-radius: 3px;
  box-shadow: 0 1px 2px rgba(0,0,0,0.08);
  position: absolute;
  z-index: 2000;
  pointer-events: auto;
  box-sizing: border-box;
  touch-action: none;
}

/* Показываем ручки при selection / editing / focus-within */
.el.selected .handle,
.el.editing .handle,
.el:focus-within .handle {
  display: block;
}

/* Позиционирование ручек и курсоры */
.handle-nw { left: -8px; top: -8px; cursor: nwse-resize; }
.handle-ne { right: -8px; top: -8px; cursor: nesw-resize; }
.handle-se { right: -8px; bottom: -8px; cursor: nwse-resize; }
.handle-sw { left: -8px; bottom: -8px; cursor: nesw-resize; }

.handle-n { left: 50%; transform: translateX(-50%); top: -8px; cursor: ns-resize; }
.handle-s { left: 50%; transform: translateX(-50%); bottom: -8px; cursor: ns-resize; }
.handle-e { right: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }
.handle-w { left: -8px; top: 50%; transform: translateY(-50%); cursor: ew-resize; }

/* Hover/focus visual for handles */
.handle:hover,
.handle:focus {
  background: #f7f9fc;
  border-color: #666;
  box-shadow: 0 2px 6px rgba(0,0,0,0.15);
}

/* Accessibility: larger hit area for touch devices */
@media (pointer: coarse) {
  .handle { width: 24px; height: 24px; left: -12px; top: -12px; }
  .handle-ne { right: -12px; top: -12px; }
  .handle-se { right: -12px; bottom: -12px; }
  .handle-sw { left: -12px; bottom: -12px; }
  .handle-n { top: -12px; }
  .handle-s { bottom: -12px; }
  .handle-e { right: -12px; }
  .handle-w { left: -12px; }
}

/* Responsive tweak */
@media (max-width: 800px) {
  .workspace { width: 100%; height: 320px; }
}

/* Утилиты/дебаг (при необходимости раскомментировать) */
/*
.workspace .el .handle.debug-show {
  display: block !important;
  background: rgba(255,80,80,0.9) !important;
  z-index: 99999 !important;
}
*/

/* Workspace base (keep as before but remove inline child styling) */
.workspace { position: relative; width: 600px; height: 400px; border: 1px solid #999; overflow: hidden; }

/* other app-level classes unchanged (toolbar, slide-list, etc) */

/* Debug helper (comment out in production) */
/*
.workspace .el .handle.debug-show {
  display: block !important;
  background: rgba(255,80,80,0.9) !important;
  z-index: 99999 !important;
}
*/
